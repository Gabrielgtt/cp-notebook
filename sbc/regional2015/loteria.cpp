#include <bits/stdc++.h>
#define f first
#define s second
using namespace std;

const int MAXN = 10020;
typedef long long ll;
typedef pair <string, ll> si;

int soma[MAXN];

int main(){
	int n, k;
	scanf("%d %d", &n, &k);
	
	int num;
	for (int i=0; i<n; i++){
		for (int j=0; j<k; j++){
			scanf("%d", &num);
		}
	}

	return 0;
}